import{sql}from"drizzle-orm";import{index,integer,sqliteTable,text}from"drizzle-orm/sqlite-core";
export const factoryWorkOrders=sqliteTable("factory_work_orders",{id:integer("id").primaryKey({autoIncrement:true}),assignmentId:integer("assignment_id").notNull().unique(),drawingId:integer("drawing_id").notNull(),windowId:integer("window_id").notNull(),measurementVersionId:integer("measurement_version_id").notNull(),drawingVersion:integer("drawing_version").notNull(),drawingSha256:text("drawing_sha256").notNull(),factoryName:text("factory_name").notNull(),factoryCode:text("factory_code").notNull().default(""),factoryOrderNumber:text("factory_order_number").notNull().default(""),status:text("status").notNull().default("queued"),expectedCompletionAt:text("expected_completion_at"),completedAt:text("completed_at"),notes:text("notes").notNull().default(""),createdBy:integer("created_by").notNull(),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`),updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>({windowIdx:index("factory_work_orders_window_idx").on(t.windowId,t.createdAt),statusIdx:index("factory_work_orders_status_idx").on(t.status,t.expectedCompletionAt)}));
export const factoryWorkOrderFreight=sqliteTable("factory_work_order_freight",{id:integer("id").primaryKey({autoIncrement:true}),workOrderId:integer("work_order_id").notNull(),carrier:text("carrier").notNull().default(""),trackingNumber:text("tracking_number").notNull().default(""),currency:text("currency").notNull().default("USD"),amountCents:integer("amount_cents").notNull().default(0),status:text("status").notNull().default("planned"),shippedAt:text("shipped_at"),receivedAt:text("received_at"),notes:text("notes").notNull().default(""),createdBy:integer("created_by").notNull(),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`),updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>({workOrderIdx:index("factory_work_order_freight_work_order_idx").on(t.workOrderId,t.createdAt),trackingIdx:index("factory_work_order_freight_tracking_idx").on(t.trackingNumber)}));
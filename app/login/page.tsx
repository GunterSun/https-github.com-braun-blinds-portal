"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import en from "@/locales/en/auth.json";import zh from "@/locales/zh-CN/auth.json";
export default function LoginPage(){const [locale,setLocale]=useState<"en"|"zh-CN">("zh-CN"),copy=locale==="zh-CN"?zh:en,[login,setLogin]=useState(""),[password,setPassword]=useState(""),[showPassword,setShowPassword]=useState(false),[saving,setSaving]=useState(false),[error,setError]=useState("");const submit=async(event:FormEvent)=>{event.preventDefault();if(saving)return;setSaving(true);setError("");try{const response=await fetch("/api/v4/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({login,password})}),data=await response.json();if(!response.ok)throw new Error(data.error||copy.loginFailed);await fetch("/api/v4/auth/me",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({preferredLocale:locale})});location.href="/hub"}catch(reason){setError(reason instanceof Error?reason.message:copy.loginFailed);setSaving(false)}};return <main className="page"><form className="card" onSubmit={submit}><button className="language" type="button" onClick={()=>setLocale(locale==="zh-CN"?"en":"zh-CN")}>{copy.language}</button><small>BRAUN SMART PORTAL · V4</small><h1>{copy.loginTitle}</h1><p>{copy.loginDescription}</p><label>{copy.loginField}<input autoComplete="username" required value={login} onChange={event=>setLogin(event.target.value)}/></label><label>{copy.password}<span className="password-field"><input autoComplete="current-password" required type={showPassword?"text":"password"} value={password} onChange={event=>setPassword(event.target.value)}/><button className="reveal" type="button" aria-pressed={showPassword} onClick={()=>setShowPassword(value=>!value)}>{showPassword?copy.hide:copy.show}</button></span></label>{error&&<div className="error">{error}</div>}<button disabled={saving}>{saving?copy.signingIn:copy.signIn}</button><div className="links"><Link href="/setup-owner">{copy.firstOwner}</Link><Link href="/">{copy.legacy}</Link></div></form><style jsx>{styles}</style></main>}
const styles=`.page{min-height:100vh;display:grid;place-items:center;background:#f4f1ea;color:#173f35;padding:20px;font-family:Arial,"PingFang SC",sans-serif}.card{position:relative;box-sizing:border-box;width:min(460px,100%);background:#fff;border:1px solid #dcd8cd;border-radius:20px;padding:28px;box-shadow:0 20px 60px rgba(25,55,45,.1)}small{letter-spacing:1px;color:#6b7a74}h1{margin:9px 0;font-size:29px}p{color:#68746e;line-height:1.6}label{display:grid;gap:7px;margin-top:15px;font-size:13px;font-weight:800}input{box-sizing:border-box;width:100%;padding:12px;border:1px solid #cbd5cf;border-radius:10px;font:inherit}.password-field{display:grid;grid-template-columns:1fr auto;gap:8px}.reveal,.language{width:auto;margin:0;padding:0 14px;background:#e6eee9;color:#174f3e}.language{position:absolute;right:20px;top:18px;padding:8px 11px}button{width:100%;border:0;border-radius:10px;padding:12px;margin-top:18px;background:#1f5b49;color:#fff;font-weight:800;cursor:pointer}button:disabled{opacity:.5}.error{background:#fff0ec;color:#963c31;padding:11px;border-radius:9px;margin-top:14px}.links{display:flex;justify-content:space-between;gap:12px;margin-top:18px;flex-wrap:wrap}.links a{color:#1f5b49;font-size:13px}`;

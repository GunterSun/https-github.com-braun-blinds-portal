import { and, desc, eq, inArray, max } from "drizzle-orm";
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/db";
import { customerPropertyAccess, customerQuoteVersions, workflowArtifacts, workflowHandoffs } from "@/db/schema";
import { getCurrentAppUser, sha256, writeAuditLog } from "@/lib/v4-auth";

type Option={id:string;labelEn:string;labelZh:string;priceDelta:number};
const publicQuote=(q:typeof customerQuoteVersions.$inferSelect)=>({id:q.id,quoteNumber:q.quoteNumber,version:q.version,propertyId:q.propertyId,status:q.status,currency:q.currency,subtotal:q.subtotal,discountAmount:q.discountAmount,taxAmount:q.taxAmount,installationFee:q.installationFee,depositRequired:q.depositRequired,total:q.total,balance:q.total-q.depositRequired,terms:q.terms,validUntil:q.validUntil,renderingUrls:JSON.parse(q.renderingUrlsJson),options:JSON.parse(q.optionsJson),selectedOptionIds:JSON.parse(q.selectedOptionIdsJson),source:JSON.parse(q.sourceSnapshotJson),documentSha256:q.documentSha256,createdAt:q.createdAt,signedAt:q.signedAt});
const cleanMoney=(value:unknown)=>Math.round(Number(value)*100)/100;

export async function GET(){const user=await getCurrentAppUser();if(!user)return NextResponse.json({error:"Authentication required"},{status:401});const db=await getDb();if(user.role==="owner"){const handoffs=await db.select().from(workflowHandoffs).orderBy(desc(workflowHandoffs.createdAt)).limit(500),quotes=await db.select().from(customerQuoteVersions).orderBy(desc(customerQuoteVersions.createdAt)).limit(500);return NextResponse.json({handoffs:handoffs.map(h=>({id:h.id,revision:h.revision,source:JSON.parse(h.sourceSnapshotJson)})),quotes:quotes.map(publicQuote)})}if(user.role!=="customer")return NextResponse.json({error:"Permission denied"},{status:403});const grants=await db.select({propertyId:customerPropertyAccess.propertyId}).from(customerPropertyAccess).where(and(eq(customerPropertyAccess.userId,user.id),eq(customerPropertyAccess.status,"active"))),ids=grants.map(x=>x.propertyId);if(!ids.length)return NextResponse.json({quotes:[]});const quotes=await db.select().from(customerQuoteVersions).where(and(inArray(customerQuoteVersions.propertyId,ids),inArray(customerQuoteVersions.status,["issued","option_selected","signed","declined"]))).orderBy(desc(customerQuoteVersions.createdAt));return NextResponse.json({quotes:quotes.map(publicQuote)})}

export async function POST(request:NextRequest){const user=await getCurrentAppUser();if(!user||user.role!=="owner")return NextResponse.json({error:"Owner only"},{status:403});let body:Record<string,unknown>;try{body=await request.json()}catch{return NextResponse.json({error:"Invalid request"},{status:400})}const handoffId=Number(body.handoffId),subtotal=cleanMoney(body.subtotal),discount=cleanMoney(body.discountAmount||0),tax=cleanMoney(body.taxAmount||0),installation=cleanMoney(body.installationFee||0),deposit=cleanMoney(body.depositRequired||0),currency=String(body.currency||"USD"),validUntil=String(body.validUntil||""),terms=String(body.terms||"").slice(0,4000),renderings=(Array.isArray(body.renderingUrls)?body.renderingUrls:[]).map(String).filter((x:string)=>/^https:\/\//.test(x)).slice(0,10),options=(Array.isArray(body.options)?body.options:[])as Option[];if(!handoffId||![subtotal,discount,tax,installation,deposit].every(Number.isFinite)||!['USD','CNY'].includes(currency)||!/^\d{4}-\d{2}-\d{2}/.test(validUntil))return NextResponse.json({error:"Valid handoff, amounts, currency and validity required"},{status:400});const normalizedOptions=options.map((x,i)=>({id:String(x.id||`option-${i+1}`).slice(0,60),labelEn:String(x.labelEn||"").slice(0,200),labelZh:String(x.labelZh||"").slice(0,200),priceDelta:cleanMoney(x.priceDelta||0)}));if(normalizedOptions.some(x=>!x.labelEn||!x.labelZh||!Number.isFinite(x.priceDelta)))return NextResponse.json({error:"Bilingual option labels required"},{status:400});const total=cleanMoney(subtotal-discount+tax+installation);if(total<0||deposit<0||deposit>total)return NextResponse.json({error:"Invalid customer-facing totals"},{status:400});const db=await getDb(),handoff=(await db.select().from(workflowHandoffs).where(eq(workflowHandoffs.id,handoffId)).limit(1))[0];if(!handoff)return NextResponse.json({error:"Handoff not found"},{status:404});const source=JSON.parse(handoff.sourceSnapshotJson),customerId=Number(source.property?.customerId),propertyId=Number(source.property?.id);if(!customerId||!propertyId)return NextResponse.json({error:"Handoff Property must be linked to Customer"},{status:409});const quoteNumber=`Q-${String(handoff.id).padStart(5,"0")}`,latest=await db.select({v:max(customerQuoteVersions.version)}).from(customerQuoteVersions).where(eq(customerQuoteVersions.quoteNumber,quoteNumber)),version=Number(latest[0]?.v||0)+1,document={quoteNumber,version,sourceHash:handoff.sourceHash,source,customerVisible:{currency,subtotal,discountAmount:discount,taxAmount:tax,installationFee:installation,depositRequired:deposit,total,terms,validUntil,renderings,options}},documentSha256=await sha256(JSON.stringify(document)),created=(await db.insert(customerQuoteVersions).values({quoteNumber,version,customerId,propertyId,handoffId,currency,subtotal,discountAmount:discount,taxAmount:tax,installationFee:installation,depositRequired:deposit,total,terms,validUntil,renderingUrlsJson:JSON.stringify(renderings),optionsJson:JSON.stringify(normalizedOptions),sourceSnapshotJson:JSON.stringify(source),documentSha256,createdBy:user.id}).returning())[0];await db.update(workflowArtifacts).set({status:"issued",externalRecordId:String(created.id)}).where(and(eq(workflowArtifacts.handoffId,handoffId),eq(workflowArtifacts.artifactType,"quote")));await writeAuditLog({userId:user.id,action:"customer_quote_issued",entityType:"customer_quote_version",entityId:String(created.id),details:{quoteNumber,version,handoffId,documentSha256}});return NextResponse.json({quote:publicQuote(created)},{status:201})}

export async function PATCH(request:NextRequest){const user=await getCurrentAppUser();if(!user||user.role!=="customer")return NextResponse.json({error:"Customer only"},{status:403});const body=await request.json(),quoteId=Number(body.quoteId),selected:string[]=(Array.isArray(body.selectedOptionIds)?body.selectedOptionIds:[]).map((x:unknown)=>String(x));const db=await getDb(),quote=(await db.select().from(customerQuoteVersions).where(eq(customerQuoteVersions.id,quoteId)).limit(1))[0];if(!quote||!["issued","option_selected"].includes(quote.status))return NextResponse.json({error:"Editable Quote not found"},{status:404});const grant=(await db.select().from(customerPropertyAccess).where(and(eq(customerPropertyAccess.userId,user.id),eq(customerPropertyAccess.propertyId,quote.propertyId),eq(customerPropertyAccess.status,"active"))).limit(1))[0];if(!grant)return NextResponse.json({error:"Quote not found"},{status:404});const options=JSON.parse(quote.optionsJson)as Option[],allowed=new Set(options.map(x=>x.id));if(selected.some(x=>!allowed.has(x)))return NextResponse.json({error:"Invalid option"},{status:400});const hash=await sha256(JSON.stringify({...publicQuote(quote),selectedOptionIds:selected}));await db.update(customerQuoteVersions).set({selectedOptionIdsJson:JSON.stringify(selected),status:"option_selected",documentSha256:hash}).where(eq(customerQuoteVersions.id,quoteId));await writeAuditLog({userId:user.id,action:"customer_quote_options_selected",entityType:"customer_quote_version",entityId:String(quoteId),details:{selected}});return NextResponse.json({ok:true,documentSha256:hash})}

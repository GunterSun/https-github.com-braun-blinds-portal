"use client";import Link from"next/link";import{useEffect,useState}from"react";type P={id:number;name:string;address:string};type Check={key:string;labelZh:string;labelEn:string;pass:boolean;evidence:string};export default function Page(){const[properties,setProperties]=useState<P[]>([]),[propertyId,setPropertyId]=useState(0),[data,setData]=useState<{property?:P;checks?:Check[];ready?:boolean;summary?:Record<string,number>}>({}),[lang,setLang]=useState<"zh"|"en">("zh"),[busy,setBusy]=useState(false);useEffect(()=>{fetch("/api/v4/customer-journey-audit").then(r=>r.json()).then(d=>{setProperties(d.properties||[]);setPropertyId(d.properties?.[0]?.id||0)})},[]);const run=async()=>{if(!propertyId)return;setBusy(true);const d=await(await fetch(`/api/v4/customer-journey-audit?propertyId=${propertyId}`,{cache:"no-store"})).json();setData(d);setBusy(false)};return <main><header><div><small>SPRINT 9 · PARENT #147</small><h1>客户全旅程真实性检查</h1><p>Customer Journey Truth Audit — 只认可数据库中的权威证据，不推断完成状态。</p></div><nav><Link href="/hub">Hub</Link><button onClick={()=>setLang(lang==="zh"?"en":"zh")}>{lang==="zh"?"English":"中文"}</button></nav></header><section className="toolbar"><select value={propertyId} onChange={e=>setPropertyId(+e.target.value)}>{properties.map(p=><option value={p.id} key={p.id}>{p.name} · {p.address}</option>)}</select><button disabled={!propertyId||busy} onClick={()=>void run()}>{busy?"Checking…":"Run authoritative audit"}</button></section>{data.checks&&<><section className={`result ${data.ready?"pass":"blocked"}`}><h2>{data.ready?"READY / 已具备验收证据":"NOT READY / 尚缺真实证据"}</h2><p>{data.property?.name} · {data.property?.address}</p></section><section className="checks">{data.checks.map(c=><article key={c.key} className={c.pass?"pass":"fail"}><span>{c.pass?"✓":"!"}</span><div><h3>{lang==="zh"?c.labelZh:c.labelEn}</h3><p>{c.evidence}</p></div></article>)}</section><section className="summary">{Object.entries(data.summary||{}).map(([k,v])=><div key={k}><b>{v}</b><span>{k}</span></div>)}</section></>}<style jsx>{`main{min-height:100vh;background:#f3f0e9;color:#173c31;padding:20px;font-family:Arial,"PingFang SC",sans-serif}header,.toolbar,.result,.checks,.summary{max-width:1100px;margin:auto;box-sizing:border-box}header{background:#153f34;color:white;border-radius:20px;padding:24px;display:flex;justify-content:space-between}nav{display:flex;gap:8px}a,button{border:0;border-radius:9px;padding:10px 12px;background:#dfb45c;color:#173f35;text-decoration:none;font-weight:800}.toolbar{margin-top:14px;display:flex;gap:8px}.toolbar select{flex:1;padding:11px}.result{margin-top:14px;border-radius:16px;padding:18px;background:white}.result.pass{border-left:9px solid #23805b}.result.blocked{border-left:9px solid #b54b3d}.checks{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:14px}.checks article{background:white;border:1px solid #ddd8cd;border-radius:14px;padding:16px;display:flex;gap:12px}.checks article>span{width:34px;height:34px;border-radius:50%;display:grid;place-items:center;color:white;font-weight:900}.checks .pass>span{background:#23805b}.checks .fail>span{background:#b54b3d}.checks h3{margin:0}.checks p{color:#66766f}.summary{display:flex;gap:10px;flex-wrap:wrap;margin-top:14px}.summary div{background:white;padding:13px;border-radius:12px;min-width:100px}.summary b,.summary span{display:block}@media(max-width:650px){main{padding:10px}header{display:block}nav{margin-top:10px}.toolbar,.checks{grid-template-columns:1fr;display:grid}}`}</style></main>}

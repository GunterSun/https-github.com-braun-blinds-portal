"use client";import Link from"next/link";import{FormEvent,useEffect,useState}from"react";type H={id:number;revision:number;source:{property:{name:string};room:{name:string};window:{code:string};design:{version:number;combinationType:string}}};type Q={id:number;quoteNumber:string;version:number;status:string;total:number;currency:string};export default function Page(){const[handoffs,setHandoffs]=useState<H[]>([]),[quotes,setQuotes]=useState<Q[]>([]),[message,setMessage]=useState(""),[form,setForm]=useState({handoffId:0,currency:"USD",subtotal:0,discountAmount:0,taxAmount:0,installationFee:0,depositRequired:0,validUntil:new Date(Date.now()+30*86400000).toISOString().slice(0,10),terms:"Valid for 30 days. Deposit required before order conversion.",renderingUrls:"",options:""});const load=()=>fetch("/api/v4/customer-quotes",{cache:"no-store"}).then(r=>r.json()).then(d=>{setHandoffs(d.handoffs||[]);setQuotes(d.quotes||[]);setForm(x=>({...x,handoffId:x.handoffId||d.handoffs?.[0]?.id||0}))});useEffect(()=>{void load()},[]);const issue=async(e:FormEvent)=>{e.preventDefault();let options:unknown[]=[];try{options=form.options.trim()?JSON.parse(form.options):[]}catch{setMessage("Options must be valid JSON");return}const r=await fetch("/api/v4/customer-quotes",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...form,renderingUrls:form.renderingUrls.split("\n").map(x=>x.trim()).filter(Boolean),options})}),d=await r.json();setMessage(r.ok?`${d.quote.quoteNumber} V${d.quote.version} issued`:d.error);if(r.ok)await load()};return <main className="page"><header><div><small>SPRINT 9 · QUOTE ISSUANCE #149</small><h1>签发客户 Quote / Issue Quote</h1><p>从权威 Handoff 建立客户可见版本；不读取或公开内部成本与利润。</p></div><nav><Link href="/workflow-handoff">Handoff</Link><Link href="/hub">Hub</Link></nav></header><form className="card form" onSubmit={issue}><h2>New immutable Quote version</h2><label>Source<select value={form.handoffId} onChange={e=>setForm({...form,handoffId:+e.target.value})}>{handoffs.map(h=><option value={h.id} key={h.id}>{h.source.property.name} · {h.source.room.name}/{h.source.window.code} · Design V{h.source.design.version} · Handoff R{h.revision}</option>)}</select></label><label>Currency<select value={form.currency} onChange={e=>setForm({...form,currency:e.target.value})}><option>USD</option><option>CNY</option></select></label>{["subtotal","discountAmount","taxAmount","installationFee","depositRequired"].map(k=><label key={k}>{k}<input min="0" step="0.01" type="number" value={form[k as keyof typeof form]} onChange={e=>setForm({...form,[k]:+e.target.value})}/></label>)}<label>Valid until<input type="date" required value={form.validUntil} onChange={e=>setForm({...form,validUntil:e.target.value})}/></label><label className="wide">Terms<textarea value={form.terms} onChange={e=>setForm({...form,terms:e.target.value})}/></label><label className="wide">Rendering URLs · one HTTPS URL per line<textarea value={form.renderingUrls} onChange={e=>setForm({...form,renderingUrls:e.target.value})}/></label><label className="wide">Bilingual options JSON<textarea placeholder='[{"id":"A","labelEn":"Blackout lining","labelZh":"遮光衬布","priceDelta":120}]' value={form.options} onChange={e=>setForm({...form,options:e.target.value})}/></label><button>Issue customer Quote</button></form>{message&&<p className="message">{message}</p>}<section className="card"><h2>Issued history</h2>{quotes.map(q=><article key={q.id}><b>{q.quoteNumber} · V{q.version}</b><span>{q.status}</span><strong>{q.currency} {q.total.toFixed(2)}</strong></article>)}</section><style jsx>{styles}</style></main>};
const styles=`.page{min-height:100vh;background:#f3f0e9;color:#20372f;padding:20px;font-family:Arial,"PingFang SC",sans-serif}header,.card,.message{max-width:1150px;margin:auto;box-sizing:border-box}header{background:#153f34;color:white;border-radius:20px;padding:24px;display:flex;justify-content:space-between}nav{display:flex;gap:8px;align-items:start}a,button{border:0;border-radius:9px;padding:10px 12px;background:#dfb45c;color:#173f35;text-decoration:none;font-weight:800}.card{margin-top:14px;background:white;border:1px solid #ddd7ca;border-radius:16px;padding:18px}.form{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.form h2,.wide{grid-column:1/-1}.form label{display:grid;gap:6px;font-weight:700}.form input,.form select,.form textarea{padding:10px;border:1px solid #bdc9c3;border-radius:8px}.form textarea{min-height:70px}.form>button{background:#1e6650;color:white}.message{padding:10px;background:#eaf2ed;margin-top:12px}article{display:flex;justify-content:space-between;border-top:1px solid #e2e5df;padding:12px}@media(max-width:700px){.page{padding:10px}header{flex-direction:column}.form{grid-template-columns:1fr}.form h2,.wide{grid-column:1}}`;

"use client";import Link from"next/link";import{useEffect,useState}from"react";type Value={fieldKey:string;totalSixteenths:number};type Option={id:string;labelEn:string;labelZh:string;priceDelta:number};type Quote={id:number;quoteNumber:string;version:number;status:string;currency:string;subtotal:number;discountAmount:number;taxAmount:number;installationFee:number;depositRequired:number;total:number;balance:number;terms:string;validUntil:string;renderingUrls:string[];options:Option[];selectedOptionIds:string[];documentSha256:string;signedAt:string|null;source:{property:{name:string};room:{name:string};window:{code:string};measurement:{version:number;values:Value[]};design:{version:number;combinationType:string}}};const inches=(n:number)=>{const whole=Math.floor(n/16),r=n%16;if(!r)return `${whole}”`;const gcd=(a:number,b:number):number=>b?gcd(b,a%b):a,d=gcd(r,16);return `${whole?`${whole} `:""}${r/d}/${16/d}”`};export default function Page(){const[quotes,setQuotes]=useState<Quote[]>([]),[lang,setLang]=useState<"zh"|"en">("zh"),[message,setMessage]=useState(""),[signing,setSigning]=useState<number|null>(null),[name,setName]=useState(""),[signature,setSignature]=useState(""),[accepted,setAccepted]=useState(false);const load=()=>fetch("/api/v4/customer-quotes",{cache:"no-store"}).then(r=>r.json()).then(d=>setQuotes(d.quotes||[]));useEffect(()=>{void load()},[]);const select=async(q:Quote,id:string)=>{const selected=q.selectedOptionIds.includes(id)?q.selectedOptionIds.filter(x=>x!==id):[...q.selectedOptionIds,id],r=await fetch("/api/v4/customer-quotes",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({quoteId:q.id,selectedOptionIds:selected})});setMessage(r.ok?"Selection saved":(await r.json()).error);await load()},sign=async(q:Quote)=>{const r=await fetch("/api/v4/customer-quotes/sign",{method:"POST",headers:{"Content-Type":"application/json","Idempotency-Key":`sign-${crypto.randomUUID()}-${crypto.randomUUID()}`},body:JSON.stringify({quoteId:q.id,printedName:name,signatureText:signature,timezone:Intl.DateTimeFormat().resolvedOptions().timeZone,acceptDisclaimer:accepted})}),d=await r.json();setMessage(r.ok?"Quote signed and locked / Quote 已签署并锁定":d.error);if(r.ok){setSigning(null);await load()}};return <main className="page"><header><div><small>SPRINT 9 · CUSTOMER QUOTE #149</small><h1>{lang==="zh"?"报价审核与签署":"Quote review & signature"}</h1><p>{lang==="zh"?"尺寸、设计版本、效果图与价格均来自同一份权威快照。":"Measurements, design, renderings and price reference one authoritative snapshot."}</p></div><nav><button onClick={()=>setLang(lang==="zh"?"en":"zh")}>{lang==="zh"?"English":"中文"}</button><Link href="/customer-portal">Properties</Link></nav></header>{message&&<p className="message">{message}</p>}{!quotes.length&&<section className="card">No issued Quotes / 尚无已签发报价</section>}{quotes.map(q=>{const chosen=q.options.filter(o=>q.selectedOptionIds.includes(o.id)).reduce((s,o)=>s+o.priceDelta,0),final=q.total+chosen,values=new Map(q.source.measurement.values.map(v=>[v.fieldKey,v.totalSixteenths]));return <section className="card" key={q.id}><div className="title"><div><small>{q.quoteNumber} · V{q.version} · SHA {q.documentSha256.slice(0,12)}…</small><h2>{q.source.property.name} · {q.source.room.name}/{q.source.window.code}</h2><p>{q.source.design.combinationType} · Design V{q.source.design.version} · Measure V{q.source.measurement.version}</p></div><strong>{q.status.toUpperCase()}</strong></div><div className="dimensions">{["width_left","width_center","width_right","height_left","height_center","height_right"].map(k=><span key={k}><small>{k}</small><b>{values.has(k)?inches(values.get(k)!):"—"}</b></span>)}</div>{q.renderingUrls.length>0&&<div className="renderings">{q.renderingUrls.map((url,i)=><figure key={url}><img src={url} alt={`Approved rendering ${i+1}`}/><figcaption>{i===0?"Before / Existing":"Option / 效果图"}</figcaption></figure>)}</div>}<div className="options">{q.options.map(o=><button disabled={q.status==="signed"} className={q.selectedOptionIds.includes(o.id)?"selected":""} key={o.id} onClick={()=>void select(q,o.id)}><b>{lang==="zh"?o.labelZh:o.labelEn}</b><span>{o.priceDelta>=0?"+":""}{q.currency} {o.priceDelta.toFixed(2)}</span></button>)}</div><div className="totals"><span>Subtotal <b>{q.currency} {q.subtotal.toFixed(2)}</b></span><span>Discount <b>−{q.discountAmount.toFixed(2)}</b></span><span>Tax <b>{q.taxAmount.toFixed(2)}</b></span><span>Installation <b>{q.installationFee.toFixed(2)}</b></span><span>Selected options <b>{chosen.toFixed(2)}</b></span><span className="grand">Total <b>{q.currency} {final.toFixed(2)}</b></span><span>Deposit <b>{q.depositRequired.toFixed(2)}</b></span><span>Balance <b>{(final-q.depositRequired).toFixed(2)}</b></span></div><p className="terms">Valid until {q.validUntil} · {q.terms}</p>{q.status!=="signed"?<>{signing===q.id?<div className="signature"><input placeholder="Printed name" value={name} onChange={e=>setName(e.target.value)}/><input placeholder="Type full legal signature" value={signature} onChange={e=>setSignature(e.target.value)}/><label><input type="checkbox" checked={accepted} onChange={e=>setAccepted(e.target.checked)}/> I accept this exact Quote version, selected options, terms and electronic-signature disclosure.</label><button onClick={()=>void sign(q)}>Sign and lock exact version</button></div>:<button className="approve" onClick={()=>setSigning(q.id)}>Review disclosure and sign</button>}</>:<p className="signed">✓ Signed {q.signedAt&&new Date(q.signedAt).toLocaleString()}</p>}</section>})}<style jsx>{styles}</style></main>};
const styles=`.page{min-height:100vh;background:#f3f0e9;color:#20372f;padding:20px;font-family:Arial,"PingFang SC",sans-serif}header,.card,.message{max-width:1150px;margin:auto;box-sizing:border-box}header{background:#153f34;color:white;border-radius:20px;padding:24px;display:flex;justify-content:space-between}nav{display:flex;gap:8px;align-items:start}nav a,button{border:0;border-radius:9px;padding:10px 12px;background:#dfb45c;color:#173f35;text-decoration:none;font-weight:800}.card{margin-top:14px;background:white;border:1px solid #ddd7ca;border-radius:16px;padding:18px}.message{padding:10px;background:#eaf2ed;margin-top:12px}.title{display:flex;justify-content:space-between}.title h2{margin:5px 0}.dimensions{display:grid;grid-template-columns:repeat(6,1fr);gap:8px}.dimensions span{background:#f1f4f1;padding:9px;border-radius:8px;display:grid}.renderings{display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin-top:14px}.renderings figure{margin:0}.renderings img{width:100%;aspect-ratio:16/10;object-fit:cover;border-radius:12px}.renderings figcaption{text-align:center}.options{display:flex;gap:8px;flex-wrap:wrap;margin:14px 0}.options button{display:grid;background:#edf2ee}.options .selected{background:#1e6650;color:white}.totals{margin-left:auto;width:min(430px,100%);display:grid;gap:5px}.totals span{display:flex;justify-content:space-between;border-top:1px solid #e0e4df;padding-top:6px}.totals .grand{font-size:20px}.terms{background:#f5f4ef;padding:12px}.signature{display:grid;gap:9px;background:#edf3ef;padding:14px;border-radius:12px}.signature input{padding:10px}.signature button,.approve{background:#1e6650;color:white}.signed{color:#17603f;font-weight:900}@media(max-width:700px){.page{padding:10px}header{flex-direction:column}.dimensions{grid-template-columns:repeat(2,1fr)}.renderings{grid-template-columns:1fr}}`;
